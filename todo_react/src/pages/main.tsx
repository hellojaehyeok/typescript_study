import React from 'react';
import styled from 'styled-components'

const Main = ({}) => {
    
    return(
        <Container>
            To do list
        </Container>
    )
};

export default Main;

const Container = styled.div`
`